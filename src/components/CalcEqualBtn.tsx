import styled from 'styled-components';

export default function CalcEqualBtn() {
  return <Root>2</Root>;
}

const Root = styled.div`
  width: 240px;
  height: 60px;

  background-color: lightgrey;
`;
